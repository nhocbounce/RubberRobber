using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Note : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }

    //Note nếu cần rebuild project
    //      Điều chỉnh Line Renderer giống 100% ( Corner Vertices, End Cap Vertices, Max width)
    //      Điều chỉnh Camera giống 100% (Rendering Path, MSAA)
}
